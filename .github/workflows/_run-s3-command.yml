name: S3 Command Runner
on:
  workflow_dispatch:
    inputs:
      cmd:
        description: "Comando a executar"
        required: true
        default: "python3 scripts/s3/create_market_from_template.py TPL-YESNO-01 'Pergunta Interna 001'"
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - Ibd71901bbe5b1630ceea73d27597364c9af683 # pinned (was v4)
      - run: sudo apt-get update && sudo apt-get install -y python3 jq
      - run: |
          set -euo pipefail
          mkdir -p out/s3_gatecheck
          echo "Executando: ${{ github.event.inputs.cmd }}"
          eval ${{ github.event.inputs.cmd }} | tee /tmp/cmd.out
          echo "---"
          echo "Conteúdo de out/s3_gatecheck:"; ls -la out/s3_gatecheck || true
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # pinned (was v4)
        with:
          name: s3-run-output
          path: |
            out/s3_gatecheck
            /tmp/cmd.out
