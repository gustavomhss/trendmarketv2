name: _mbp-s2-orr (stub)

on:
  workflow_call: {}

jobs:
  noop:
    runs-on: ubuntu-22.04
    steps:
      - name: MBP S2 ORR stub
        shell: bash
        run: |
          echo "[stub] _mbp-s2-orr.yml — safe stub"
          echo "TODO: restore real content"
