redact = true

[allowlist]
  description = "Allowlist for deterministic test key fixtures"
  files = [
    "tests/fixtures/crypto/test_ed25519_priv.pem",
    "tests/fixtures/crypto/test_ed25519_pub.pem"
  ]
