sprint_name: Q2-S7-Stabilize
gates:
  -
    id: T2
    name: Pins
    purpose: Check pinned actions
    ltl: G(pinned(actions))
    run:
      - bash -lc "python3 scripts/sprint_guard.py check-pins"
    assert_patterns:
      - "PINS:OK"
    evidence:
      -
        path: out/evidence/T2_pins/report.json
        must_exist: true

# retrigger 2025-10-31T20:44:03Z
