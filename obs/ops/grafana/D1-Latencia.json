{
  "title": "D1 — Latência p75/p95",
  "timezone": "browser",
  "panels": [
    {
      "type": "timeseries",
      "title": "p95 por operação",
      "targets": [
        {"expr": "histogram_quantile(0.95, sum by (le,op) (rate(amm_op_latency_seconds_bucket[5m])))"}
      ]
    },
    {
      "type": "timeseries",
      "title": "p75 por operação",
      "targets": [
        {"expr": "histogram_quantile(0.75, sum by (le,op) (rate(amm_op_latency_seconds_bucket[5m])))"}
      ]
    }
  ]
}
