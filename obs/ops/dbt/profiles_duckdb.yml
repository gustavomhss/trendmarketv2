ce_profile:
  target: duckdb
  outputs:
    duckdb: &duckdb_output
      type: duckdb
      path: ./out/dbt/ce.duckdb
      threads: 4
      extensions:
        - httpfs
      settings:
        memory_limit: "1GB"
        temp_directory: "./out/dbt/tmp"
    ci:
      <<: *duckdb_output
